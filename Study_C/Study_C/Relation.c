#include <stdio.h>

#if 0
int main(void)
{
	int x = 50, y = 30;

	printf("x가 y와 같은가? %d\n", x == y);
	printf("x가 y와 다른가? %d\n", x != y);
	printf("x가 y보다 큰가? %d\n", x > y);
	printf("x가 y보다 작은가? %d\n", x < y);
	printf("x에 y를 넣으면? %d\n", x = y);

	if (x == y)
	{
		//실행되는 부분
	}
	else
	{
		//
	}

	return 0;
}
#endif